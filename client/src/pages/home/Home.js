import { Grid } from '@mui/material';
import HeroSection from '../../components/HeroSection';

const Home = () => {
  return (
    <Grid>
      <HeroSection />
    </Grid>
  );
};

export default Home;
